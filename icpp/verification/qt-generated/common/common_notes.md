# 共通メモ

- `.i .` を先に実行して、カレントディレクトリのヘッダーと生成物を見つけられるようにします。
- `.cpp` は `.e file.cpp` で自動登録されます。
- `.h`、`.ui`、`.qrc` は自動登録されません。
- `Q_OBJECT` を含むコードでは `.gen` で `moc` / `uic` / `rcc` を更新します。
- GUI の表示確認は手動確認として扱います。
