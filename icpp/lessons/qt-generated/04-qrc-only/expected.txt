生成されるファイル:
- qrc_resources.cpp

出力に含まれるべき文字列:
- Added:
- function QWidget *go()

手動確認:
- `go()->show()` で resource path `:/data/message.txt` の内容が表示される
- `qrc_resources.cpp` は `widget.cpp` から include する。`.add` で直接登録しない
