---
genpdf:
  Format: book
  Title: qeyes 利用ガイド
  Subtitle: <version> 版
  Author: (株) SRA
---

# 1. 概要

`qeyes` は、マウスポインターの位置を追いかける 2 つの目を常時最前面に表示する小さな Qt アプリケーションです。

`<version>` 版では、ここにこの版の特徴を要約します。

主な特徴は次の通りです。

- <特徴 1>
- <特徴 2>
- <特徴 3>

# 2. 起動方法

ビルド済みバイナリーを起動します。

```text
$ ./build/qeyes
```

ソースからビルドする場合の一例です。

```text
$ cmake -S . -B build
$ cmake --build build
$ ./build/qeyes
```

# 3. 基本操作

## 3.1 ウィンドウを動かす

左ボタンでドラッグすると、ウィンドウ全体を移動できます。

```text
左ドラッグ: ウィンドウ移動
```

## 3.2 この版で追加された操作

ここに、この版で追加または変更された操作を書きます。

```text
<操作>: <説明>
```

## 3.3 終了する

左ボタンでダブルクリックすると終了します。

```text
左ダブルクリック: 終了
```

ウィンドウを閉じた場合もアプリケーション全体が終了します。

# 4. 設定の保存

この版で保存される設定があれば、ここに書きます。

- `<key>`

# 5. 表示の仕組み

この版での表示構成や描画方法を書きます。

## 5.1 目の構成

- <要素 1>
- <要素 2>

## 5.2 視線の追従

視線追従の仕組みを書きます。

## 5.3 再描画

再描画間隔や方式を書きます。

# 6. ウィンドウの性質

- フレームレスウィンドウ
- ツールウィンドウ
- 常に最前面
- 透過背景
- 起動時にアクティブ化しない設定

# 7. 制約と注意点

- <制約 1>
- <制約 2>

# 8. まとめ

`qeyes` `<version>` の要点をここにまとめます。
