# DECISIONS

## 採用済み

### 1. QML ツールの分類

- qmlprop と qmlprofileranalyzer は、日英 README の「QML Tools」にまとめる。
- QML 関連ツールを同じカテゴリーにするという利用者の指定に基づく。

### 2. 英語 README のリンク先

- README.en.md は英語文書へリンクする。日本語のリンク先には同じディレクトリーに `.en.md`（Cling は `.en.txt`）の英語版を作成する。
- 既存文書が英語の場合はそのまま参照し、日本語原文は保持する。
- 利用者が英語 README から各ツールの説明を英語で読めるようにするため。
- 翻訳では見出し構成と技術的内容を対応させ、コマンド・設定キーを保つ。日本語入力自体が動作例になる場合は原文を残し、英語で説明する。

### 3. 利用ガイドの英語版

- `USER_GUIDE.md` の英語版は同じディレクトリーの `USER_GUIDE.en.md` とし、日本語原文を保持する。
- 同名のガイドが複数の場所にある場合も、その場所の原文の版・内容に対応させる。別の場所にある新しい版の内容を混ぜない。
- 実際の日本語表示例や日本語への翻訳教材は、例としての意味を保つため原文を残し、英語の説明を添える。

### 4. work-templates の英語化と軽量な構成

- 利用者の指定に基づき、機能や構成を増やしすぎず、既存のシンプルな構成を維持する。`ja/`、`en/` のディレクトリー分割は行わない。
- 英語版は既存の日本語版と同じディレクトリーに `AGENTS.next.en.md`、`NEXT.en.md`、`DECISIONS.en.md`、`backlog.en.md` として配置し、日本語版は変更せず保持する。
- コピー先では `~/AGENTS.next.md` およびプロジェクトの `NEXT.md`、`DECISIONS.md`、`backlog.md` という標準名を使う。既存の参照関係と運用手順を維持するため。
- 英語 README の公開用コピー例は個人環境依存の絶対パスを避け、実行場所を明記したリポジトリー相対パスから英語版をコピーする。

### 5. ルートの作業コンテキストと配布テンプレートの区別

- ルートの `NEXT.md`、`DECISIONS.md`、`backlog.md` は、maintainer が `qt-tools` の開発に使う実運用用の作業コンテキストとして Git 管理する。複数ホスト・AI セッション間で現在の作業状態、設計判断、残課題を共有するため。
- clone した利用者がそのまま自分の作業コンテキストとして使うためのテンプレートではない。再利用用は `work-templates/` に置く。
- この区別はトップの日英 README の「Documentation」で簡潔に案内し、`work-templates` の README などへ同じ説明を重複追加しない。

## 不採用

- `work-templates` を `ja/`、`en/` に分割する案は採用しない。利用者が重視する軽量でシンプルな構成を保つため、英語版を同じディレクトリーへ配置する。

## まだ決めていないこと

- 今回の依頼範囲ではなし。

## 判断基準

- README.en.md は README.md の内容に沿った自然な英語にする。
- ツールの説明は各ツールの README や利用ガイドに基づく。

## 継続する仕様・運用制約

- 日英 README のカテゴリーと掲載ツールを一致させ、リンク先は同じ文書の各言語版に対応させる。
- 対応環境の記述も日英で揃え、ツールごとの詳細は各ツールの文書へ案内する。
