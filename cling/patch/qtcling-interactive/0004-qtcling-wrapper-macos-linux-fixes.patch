diff --git a/bin/iqtcling.linux b/bin/iqtcling.linux
index 49e9ca2..50f1807 100755
--- a/bin/iqtcling.linux
+++ b/bin/iqtcling.linux
@@ -24,7 +24,7 @@ if [ ! -f "$QT_ROOT/lib/libQt6Core.so" ]; then
     exit 1
 fi
 
-PRELOAD_HEADER=$(mktemp "${TMPDIR:-/tmp}/qtcling-preload.XXXXXX.h") || exit 1
+PRELOAD_HEADER=$(mktemp "${TMPDIR:-/tmp}/qtcling-preload.XXXXXX") || exit 1
 trap 'rm -f "$PRELOAD_HEADER"' EXIT HUP INT TERM
 
 printf '#pragma cling load("%s")\n' "$QT_ROOT/lib/libQt6Core.so" > "$PRELOAD_HEADER"
diff --git a/bin/iqtcling.macos b/bin/iqtcling.macos
index 2a1f2f0..dea839c 100755
--- a/bin/iqtcling.macos
+++ b/bin/iqtcling.macos
@@ -33,9 +33,18 @@ else
     QT_ROOT_SOURCE=default
 fi
 
-CLING_ROOT=${QTCLING_CLING_ROOT:-/usr/local/qtcling}
-CLING=${CLING_ROOT}/bin/cling
 DEV_ROOT=/usr/local/src/cling
+if [ -n "${QTCLING_CLING_ROOT+x}" ]; then
+    CLING_ROOT=$QTCLING_CLING_ROOT
+    CLING_ROOT_SOURCE=QTCLING_CLING_ROOT
+elif [ -x "${DEV_ROOT}/build/bin/cling" ]; then
+    CLING_ROOT="${DEV_ROOT}/build"
+    CLING_ROOT_SOURCE=dev-build
+else
+    CLING_ROOT=/usr/local/qtcling
+    CLING_ROOT_SOURCE=default
+fi
+CLING=${CLING_ROOT}/bin/cling
 QT_FRAMEWORK_PATH="${QT_ROOT}/lib"
 QT_MODULES=${QTCLING_MODULES:-"Core Gui Widgets"}
 
@@ -77,6 +86,7 @@ print_config() {
     echo "  qt_root_source: $QT_ROOT_SOURCE"
     echo "  modules: $QT_MODULES"
     echo "  cling_root: $CLING_ROOT"
+    echo "  cling_root_source: $CLING_ROOT_SOURCE"
     echo "  cling: $CLING"
     echo "  dev_root: $DEV_ROOT"
     echo
@@ -158,7 +168,7 @@ if ! check_config >/dev/null 2>&1; then
     exit 1
 fi
 
-PRELOAD_HEADER=$(mktemp "${TMPDIR:-/tmp}/qtcling-preload.XXXXXX.h") || exit 1
+PRELOAD_HEADER=$(mktemp "${TMPDIR:-/tmp}/qtcling-preload.XXXXXX") || exit 1
 trap 'rm -f "$PRELOAD_HEADER"' EXIT HUP INT TERM
 
 for module in $QT_MODULES; do
