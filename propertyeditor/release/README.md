# release

このディレクトリには、配布用 source code release zip を置きます。
zip 作成用の staging は一時ディレクトリで行います。

現在の命名規則は次のとおりです。

- `propertyeditor-<version>-source.zip`

配布 zip には `build/`、`install/`、`.DS_Store`、日常作業用の一時ファイルを含めません。
